export function explorarFuncoes() {
  let resposta = `
Para explorar mais funções predefinidas do JavaScript:

1) Acesse a documentação oficial: https://developer.mozilla.org
2) Teste cada função no console do navegador.
3) Experimente funções como:
   - JSON.stringify()
   - JSON.parse()
   - Object.keys()
   - Object.values()
   - Array.map()
   - Array.filter()
   - Array.reduce()
`;
  return resposta;
}
