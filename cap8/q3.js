export function vantagemEncapsulamento() {
  return `
O encapsulamento permite deixar dados privados dentro de um módulo e expor apenas funções 
necessárias para o restante do sistema. Isso evita acessos indevidos aos dados. Exemplo:
um módulo usuariosDB expõe apenas funções de CRUD, mantendo o array oculto.
`;
}